# 에이전트 공통 지침

## 문서
- 영문 용어 뒤에 한국어 조사나 접미사를 직접 붙이지 마라
- 코드 블록에는 가급적 영어만 사용하라
- 자연스러운 한국어 표현이 있으면 영문 용어보다 우선하라

## 코드
- 주요 명령 흐름을 쉽게 읽히게 유지하라
- 넓은 추상화보다 최소 구현을 우선하라
- 운영자가 읽기 쉬운 출력을 먼저 고려하라
- 스크립트가 검사하고 기록하기 쉬운 출력을 만들어라
- 표준 약어를 제외한 사용자 로그 메시지는 소문자로 작성하라
- 영리한 형식보다 가독성을 우선하라
- 가능하면 한 줄을 88자 이내로 유지하라
- 의미가 분명하면 짧은 이름을 사용하라

## 형상 관리
- 논리적 변경 하나마다 작고 의미 있는 커밋을 만들어라
- 소문자 구글 스타일 제목을 사용하라: `add: ...`, `fix: ...`, `refactor: ...`
- 자세한 설명이 필요하지 않으면 본문 없이 제목만 작성하라
- 제목이 광범위하게 느껴지면 커밋을 나누거나 범위를 좁혀라
- 마지막 커밋의 메시지만 고칠 때는 푸시 전에 수정 커밋으로 덮어써라

## 언어별 지침

### Bash
- 지원 기준을 다음 버전으로 고정하라: `Bash 3.2`
- 모든 셸 코드는 이 버전에서 실행 가능하게 작성하라
- 루트 실행 파일에는 읽기 쉬운 명령 흐름만 남겨라
- 세부 로직은 작은 함수로 나눠 `utils/` 아래에 배치하라
- 결과는 표준 출력, 진단은 표준 오류로 출력하라
- 성공과 실패는 종료 코드로 구분하라
- 루트 명령 파일은 확장자 없이 짧은 소문자로 지어라
- 모듈 파일 경로는 다음 형식을 따르라: `utils/<name>.sh`
- 모듈 함수 이름은 `module_action` 형식을 따르라
- 지역 변수 선언은 함수 맨 위에 배치하라
- 함수 본문을 간결하게 유지하고 최상위 함수 사이에만 빈 줄을 넣어라
- 다음 실행 파일 형태를 기준으로 삼아라

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

usage() {
  echo "$0 <target>" >&2
  exit 1
}

# shellcheck source=/dev/null
source "$ROOT_DIR/utils/source.sh"
source_modules \
  utils/profile.sh \
  utils/target.sh

main() {
  local target
  [ "$#" -eq 1 ] || usage
  target=$1
  profile_prepare
  target_run "$target"
}

main "$@"
```
