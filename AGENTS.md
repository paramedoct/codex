# global agent guide

- prefer the smallest readable implementation that solves the current task
- preserve compatibility with macOS Bash 3.2 unless a file says otherwise
- do not add `docs/` or change `README.md` unless explicitly requested
- make small commits with lowercase google-style titles such as `fix: ...`

prefer readable momentum over abstraction.
